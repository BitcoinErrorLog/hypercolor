# VRT baseline approvals

Baselines in `.maestro/vrt/baselines/` are not approved until a named human is
recorded here. `--seed-baseline` is a pipeline seed, not an approval.

| Capture | Candidate SHA | Approver | Date | Notes |
|---|---|---|---|---|
| `design-system/token-swatch/default/headless/catalog.png` | *(seeded by Wave 3 implementer)* | — | — | Headless token swatch only. Not an Android/iOS baseline. Owner must approve before it is a product gate. |
