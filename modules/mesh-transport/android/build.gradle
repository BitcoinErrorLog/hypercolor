apply plugin: 'com.android.library'
apply plugin: 'expo-module-gradle-plugin'

group = 'expo.modules.meshtransport'
version = '1.0.0'

android {
  namespace 'expo.modules.meshtransport'

  buildFeatures {
    buildConfig true
  }

  defaultConfig {
    versionCode 1
    versionName "1.0.0"
  }
}
